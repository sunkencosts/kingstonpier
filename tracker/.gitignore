# Model weights (large; trained/downloaded locally)
*.pt

# Labelling dataset (frames + point labels)
dataset/

# uv-managed virtual environment
.venv/

# training run log (regenerated by train_counter.py each run)
train_log.jsonl

# local editor/agent settings
.claude/
